{
  "name": "digital-e-shop",
  "version": "1.0.0",
  "private": true,
  "description": "A full-stack e-commerce system selling electronic components and devices",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "concurrently \"pnpm --filter server start\" \"pnpm --filter client start\"",
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "vercel-build": "pnpm --filter server build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@11.5.3",
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "dependencies": {
    "claude": "^0.1.2"
  }
}
